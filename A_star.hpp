#pragma once
#include<queue>

#include"Node.hpp"

/*
template<typename T>
struct Cmp {
	bool operator()(const T& x, const T& y) const;
};

class A_star {

	Map _map;

	std::vector<Node*> _openlist;
	std::vector<Node*> _closelist;
	Node* _snode;
	Node* _gnode;

	Cost* _cost;

public:
	A_star();
	A_star(const Map& map, Cost* cost);
	A_star(const A_star& a_star);

	//A-starƒAƒ‹ƒSƒŠƒYƒ€‚ğÀs
	void Init(Node* sn, Node* gn);
	Node* Run();

	Map* getMap();
	bool Moveable(const int x, const int y);

};
*/
